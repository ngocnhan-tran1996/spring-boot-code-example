server:
  port: 8080

spring:
  application:
    name: springboot-rabbitmq

  rabbitmq:
    host: localhost
    port: 5672
    username: ngocnhan
    password: ngocnhan
    listener:
      simple:
        prefetch: 1
        retry:
          enabled: true
          max-attempts: 5
          initial-interval: 3s
          max-interval: 3s

app:
  queue:
    name: local_QUEUE_TEST
    names: local_QUEUE_TEST_1,local_QUEUE_TEST_2,local_QUEUE_TEST_3