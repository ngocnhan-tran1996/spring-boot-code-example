spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: ngocnhan
    driver-class-name: org.postgresql.Driver
    hikari:
      schema: public
      minimum-idle: 1
      maximum-pool-size: 1

  jpa:
    show-sql: true
    properties:
        hibernate:
            '[format_sql]': true