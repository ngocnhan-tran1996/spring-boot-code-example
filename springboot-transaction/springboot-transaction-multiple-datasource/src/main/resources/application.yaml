server:
  port: 8080

spring:
  application:
    name: springboot-transaction-multiple-source
  profiles:
    active: only-oracle

logging:
  level:
     org:
      springframework:
        '[orm.jpa]': TRACE
        '[transaction]': TRACE